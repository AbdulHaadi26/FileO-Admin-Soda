import React from 'react';

const Counter = ({ end_date }) => {
    //let today = new Date(Date.now());

    //let eDate = new Date(Date.now())

    return <div className="counter">

    </div>
};

export default Counter;